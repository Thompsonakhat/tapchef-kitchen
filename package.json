{
  "name": "tapchef-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:root": "bash -lc \"set -e; if [ -f package-lock.json ]; then npm ci; else npm install; fi\"",
    "build": "npm run install:root && npm run install:webapp && npm run build:webapp",
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "install:webapp": "bash -lc \"set -e; if [ -f webapp/package-lock.json ]; then npm --prefix webapp ci; else npm --prefix webapp install; fi\"",
    "build:webapp": "npm --prefix webapp run build",
    "dev:webapp": "npm --prefix webapp run dev"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "grammy": "^1.20.0",
    "mongodb": "^6.10.0",
    "@grammyjs/runner": "^2.0.3",
    "express": "latest",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {}
}
